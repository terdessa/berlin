# Claude Working Notes

Use `docs/agent-context.md` as the canonical context.

When helping with Sentinel:

- Ask one clarifying question at a time unless the user requests a batch.
- After each user answer, reflect what the answer implies and suggest sharper positioning when useful.
- Update the relevant documentation file after meaningful answers.
- Keep the writing pitch-ready, concise, and easy for another agent or teammate to reuse.
- Do not add timestamps.
- Use `knowledge/active-context.md` and `knowledge/handoff.md` to preserve current project state.
