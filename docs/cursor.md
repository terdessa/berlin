# Cursor Working Notes

Use `docs/agent-context.md` as the canonical context.

When assisting inside the editor:

- Keep project documentation under `docs/`.
- Use `clarifying-questions.md` to track the discovery process.
- Use `decision-log.md` for decisions and rationale.
- Do not add timestamps.
- Avoid duplicating the same context across many files; link back to `agent-context.md` when possible.
- Use `knowledge/active-context.md` and `knowledge/handoff.md` before making changes.
